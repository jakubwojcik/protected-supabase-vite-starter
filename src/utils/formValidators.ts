export const checkIfEmailIsValid = async (email: string) => {
  console.log(
    `This is a placeholder function that would make a request to the backend to see if the email is valid: ${email}`,
  );
  return true;
};
